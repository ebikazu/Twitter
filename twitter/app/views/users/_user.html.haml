= div_for user, class: "hoge" do |u|
	%h4
		%span.user-name
			= link_to u.name, user_path(u)
		%span.user-id
			@#{u.name}
	.bio
		= u.bio
	.follow-unfollow
		- if u.followed_by? current_user
			= link_to "フォロー解除", user_follows_path(u), method: :delete
		- else
			= link_to "フォロー", user_follows_path(u), method: :post